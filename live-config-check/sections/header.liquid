<style>
    /* slider custom css*/
     @media only screen and  (max-device-width: 344px)  {

      .slideshow__slide-content .prose {
      position: relative;
      margin-top: -95%;
  }


      .slideshow__slide-content .prose p.h1 {

          font-size: 21px;
      font-weight: bold;
      line-height: 32px;
      text-align: center;
  }
     .slideshow__slide-content .prose a.button {

      top: 48px;
      left: 22px;
      padding: 12px;
      width: 260px;
      font-size: 14px;
      font-weight: 400;
      border-radius: 13px;

  }
    }

  @media only screen and (min-device-width: 345px) and (max-device-width: 374px)   {

      .slideshow__slide-content .prose {
      position: relative;
      margin-top: -95%;
  }


      .slideshow__slide-content .prose p.h1 {

          font-size: 21px;
      font-weight: bold;
      line-height: 32px;
      text-align: center;
  }
     .slideshow__slide-content .prose a.button {

      top: 48px;
      left: 25px;
      padding: 12px;
      width: 260px;
      font-size: 14px;
      font-weight: 400;
      border-radius: 13px;

  }
    }


   @media only screen and (min-device-width: 375px) and (max-device-width: 389px)   {

      .slideshow__slide-content .prose {
      position: relative;
      margin-top: -95%;
  }


      .slideshow__slide-content .prose p.h1 {

      font-size: 21px;
      font-weight: bold;
      line-height: 32px;
      text-align: center;
  }
     .slideshow__slide-content .prose a.button {

      top: 48px;
      left: 40px;
      padding: 12px;
      width: 260px;
      font-size: 14px;
      font-weight: 400;
      border-radius: 13px;

  }
    }
    @media only screen and (min-device-width: 390px) and (max-device-width: 411px)   {

      .slideshow__slide-content .prose {
      position: relative;
      margin-top: -95%;
  }


      .slideshow__slide-content .prose p.h1 {

      font-size: 21px;
      font-weight: bold;
      line-height: 32px;
      text-align: center;
  }
     .slideshow__slide-content .prose a.button {

     top: 70px;
      left: 44px;
      padding: 12px;
      width: 260px;
      font-size: 14px;
      font-weight: 400;
      border-radius: 13px;
  }
    }


    @media only screen and (min-device-width:412px) and (max-device-width: 539px)  {

      .slideshow__slide-content .prose {
      position: relative;
      margin-top: -80%;
  }


      .slideshow__slide-content .prose p.h1 {
      font-size: 21px;
      font-weight: bold;
      line-height: 40px;
      word-spacing: -5px;
      text-align: center;
  }
        .slideshow__slide-content .prose a.button {

      top: 32px;
      left: 62px;
      padding: 12px;
      width: 260px;
      font-size: 14px;
      font-weight: 400;
      border-radius: 13px;

  }
    }
    @media only screen and (min-device-width: 540px) and (max-device-width: 767px)   {

      .slideshow__slide-content .prose {
      position: relative;
      margin-top: -80%;
  }


      .slideshow__slide-content .prose p.h1 {

      font-size: 21px;
      font-weight: bold;
      line-height: 32px;
      text-align: center;
  }
     .slideshow__slide-content .prose a.button {

      top: 70px;
      left: 106px;
      padding: 12px;
      width: 260px;
      font-size: 14px;
      font-weight: 400;
      border-radius: 13px;
    }
  }
    @media only screen and (min-device-width: 768px) and (max-device-width: 1280px)   {

      .slideshow__slide-content .prose {
     position: relative;
      margin-top: -7%;
  }


      .slideshow__slide-content .prose p.h1 {

      font-size: 21px;
      font-weight: bold;
      line-height: 32px;
      width: 330px;
  }
     .slideshow__slide-content .prose a.button {

     top: 32px;
      padding: 8px;
      width: 220px;
      font-size: 12px;
      font-weight: 400;
      border-radius: 13px;
  }

  }

     @media only screen and (min-device-width: 1023px) and (max-device-width: 1280px)   {

      .slideshow__slide-content .prose {
     position: relative;
      margin-top: -7%;
  }


      .slideshow__slide-content .prose p.h1 {

          font-size: 30px;
          font-weight: bold;
          line-height: 42px;
          width: 360px;
          letter-spacing: 2px;
  }
     .slideshow__slide-content .prose a.button {

          top: 60px;
          padding: 10px;
          width: 230px;
          font-size: 13px;
          font-weight: 400;
          border-radius: 13px;
  }

  }

    :root {
      --header-is-sticky: {% if section.settings.enable_sticky %}1{% else %}0{% endif %};
    }

    #shopify-section-{{ section.id }} {
      --header-grid: "primary-nav logo secondary-nav" / minmax(0, 1fr) auto minmax(0, 1fr);
      --header-padding-block: 1rem;
      --header-transparent-header-text-color: {{ section.settings.transparent_header_text_color.rgb }};
      --header-separation-border-color: {% if section.settings.show_separation_border %}{{ section.settings.color_scheme.settings.text_color.rgb }} / 0.15{% else %}0 0 0 / 0{% endif %};

      position: relative;
      z-index: 4;
    }

    @media screen and (min-width: 700px) {
      #shopify-section-{{ section.id }} {
        --header-padding-block: {% if section.settings.reduce_padding %}1.2rem{% else %}1.6rem{% endif %};
      }
    }

    @media screen and (min-width: 1000px) {
      #shopify-section-{{ section.id }} {
        {%- case section.settings.layout -%}
          {%- when 'logo_left_navigation_inline' -%}
            --header-grid: "logo primary-nav secondary-nav" / auto minmax(0, 1fr) auto;

          {%- when 'logo_left_navigation_center' -%}
            --header-grid: "logo primary-nav secondary-nav" / minmax(max-content, 1fr) auto minmax(max-content, 1fr);

          {%- when 'logo_center_navigation_below' -%}
            --header-grid: ". logo secondary-nav" "primary-nav primary-nav primary-nav" / minmax(0, 1fr) auto minmax(0, 1fr);
        {%- endcase -%}
      }
    }

    {%- if section.settings.enable_sticky -%}
      #shopify-section-{{ section.id }} {
        position: sticky;
        top: 0;
      }

      .shopify-section--announcement-bar ~ #shopify-section-{{ section.id }} {
        top: calc(var(--announcement-bar-is-sticky, 0) * var(--announcement-bar-height, 0px));
      }
    {%- endif -%}

    {%- if section.settings.logo != blank -%}
      #shopify-section-{{ section.id }} {
        --header-logo-width: {{ section.settings.logo_mobile_max_width }}px;
      }

      @media screen and (min-width: 700px) {
        #shopify-section-{{ section.id }} {
          --header-logo-width: {{ section.settings.logo_max_width }}px;
        }
      }
    {%- endif -%}
</style>

<height-observer variable="header">
  <x-header
    {% if section.settings.hide_on_scroll and section.settings.enable_sticky %}
      hide-on-scroll
    {% endif %}
    class="header color-scheme color-scheme--{{ section.settings.color_scheme.id }} {% unless section.settings.enable_full_width %}container{% endunless %}"
  >
    {%- comment -%}
      --------------------------------------------------------------------------------------------------------------------
      LOGO AREA
      --------------------------------------------------------------------------------------------------------------------
    {%- endcomment -%}

    {%- capture logo_content -%}
      {%- if section.settings.logo-%}
        {%- capture sizes -%}{{ section.settings.logo_max_width }}px{%- endcapture -%}
        {%- capture widths -%}{{ section.settings.logo_max_width | times: 2 | at_most: section.settings.logo.width }}, {{ section.settings.logo_max_width | times: 3 | at_most: section.settings.logo.width }}{%- endcapture -%}

        <span class="sr-only">{{ shop.name }}</span>
        {{- section.settings.logo | image_url: width: section.settings.logo.width | image_tag: sizes: sizes, widths: widths, class: 'header__logo-image' -}}

        {%- if section.settings.transparent_logo -%}
          {{- section.settings.transparent_logo | image_url: width: section.settings.transparent_logo.width | image_tag: sizes: sizes, widths: widths, class: 'header__logo-image header__logo-image--transparent' -}}
        {%- endif -%}
      {%- else -%}
        <span class="header__logo-text h5">{{ shop.name }}</span>
      {%- endif -%}
   {%- endcapture -%}
 
    {%- if request.page_type == 'index' -%}
      <h1 class="header__logo">
        <a href="{{ routes.root_url }}">{{ logo_content }}</a>
      </h1>
    {% else %}
      <a href="{{ routes.root_url }}" class="header__logo">{{ logo_content }}</a>
    {% endif %}

    {%- comment -%}
      --------------------------------------------------------------------------------------------------------------------
      PRIMARY NAVIGATION AREA
      --------------------------------------------------------------------------------------------------------------------
    {%- endcomment -%}

    {%- assign menu = section.settings.menu -%}
    {%- assign sidebar_menu = section.settings.sidebar_menu -%}

    <nav
      class="header__primary-nav {% if section.settings.layout == 'logo_center_navigation_below' or section.settings.layout == 'logo_left_navigation_center' %}header__primary-nav--center{% endif %}"
      aria-label="{{ 'header.general.primary_navigation' | t | escape }}"
    >
      <button
        type="button"
        aria-controls="sidebar-menu"
        {% unless section.settings.layout == 'drawer' %}
          class="md:hidden"
        {% endunless %}
      >
        <span class="sr-only">{{ 'header.general.open_menu' | t }}</span>
        {% render 'icon' with 'hamburger', class: 'header__nav-icon' %}
      </button>

      {% if menu.links.size > 0 and section.settings.layout != 'drawer' %}
        <ul class="contents unstyled-list md-max:hidden">
          {% for link in menu.links %}
            {% liquid
              assign link_title_downcase = link.title | strip | downcase
              assign mega_menu_block = ''

              for block in section.blocks
                assign menu_item_downcase = block.settings.menu_item | strip | downcase

                if menu_item_downcase == link_title_downcase
                  assign mega_menu_block = block
                  break
                endif
              endfor
            %}

            <li class="header__primary-nav-item" data-title="{{ link.title | escape }}">
              {% if link.levels == 0 and mega_menu_block == '' %}
                <a
                  href="{{ link.url }}"
                  class="block h6"
                  {% if link.current %}
                    aria-current="page"
                  {% endif %}
                >
                  {{- link.title -}}
                </a>
              {% else %}
                <details
                  is="{% if mega_menu_block != '' %}mega-menu-disclosure{% else %}dropdown-menu-disclosure{% endif %}"
                  class="header__menu-disclosure"
                  follow-summary-link
                  trigger="{{ section.settings.menu_open_trigger }}"
                >
                  <summary data-follow-link="{{ link.url }}" class="h6">
                    {{- link.title -}}
                  </summary>

                  {% if mega_menu_block != '' %}
                    {% render 'mega-menu', link: link, block: mega_menu_block %}
                  {% else %}
                    <ul
                      class="header__dropdown-menu {% if link.levels <= 1 %}header__dropdown-menu--restrictable{% endif %} unstyled-list"
                      role="list"
                    >
                      {% for sub_link in link.links %}
                        <li>
                          {% if sub_link.levels == 0 %}
                            <a
                              href="{{ sub_link.url }}"
                              class="link-faded-reverse"
                              {% if sub_link.current %}
                                aria-current="page"
                              {% endif %}
                            >
                              {{- sub_link.title -}}
                            </a>
                          {% else %}
                            <details
                              is="dropdown-menu-disclosure"
                              class="header__menu-disclosure"
                              follow-summary-link
                              trigger="{{ section.settings.menu_open_trigger }}"
                            >
                              <summary data-follow-link="{{ sub_link.url }}" class="link-faded-reverse">
                                <div class="h-stack gap-4 justify-between">
                                  {{- sub_link.title -}}
                                  {%- render 'icon' with 'arrow-right', width: 8, direction_aware: true -%}
                                </div>
                              </summary>

                              <ul class="header__dropdown-menu unstyled-list" role="list">
                                {% for sub_sub_link in sub_link.links %}
                                  <li>
                                    <a
                                      href="{{ sub_sub_link.url }}"
                                      class="link-faded-reverse"
                                      {% if sub_sub_link.current %}
                                        aria-current="page"
                                      {% endif %}
                                    >
                                      {{- sub_sub_link.title -}}
                                    </a>
                                  </li>
                                {% endfor %}
                              </ul>
                            </details>
                          {% endif %}
                        </li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </details>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
      {% endif %}
    </nav>

    {%- comment -%}
      --------------------------------------------------------------------------------------------------------------------
      PRO PRODUCT AREA BEGIIN
      --------------------------------------------------------------------------------------------------------------------
    {%- endcomment -%}

    {% if customer %}
      {% unless customer.tags contains 'premium' %}
        <script>
          (function() {
              var mytt = setInterval(myfunc, 50);
              function myfunc(){
                var link = document.querySelectorAll('[href="/products/dtf-gang-sheet-pro"]');
                  if(link.length>0) {
                    link[0].remove();
                    // clearInterval(mytt);
                  }
              }
          })();
        </script>
      {% endunless %}

      {% if customer.tags contains 'premium' %}
        <script>
          (function() {
             var myttt = setInterval(myfuncc, 50);
              function myfuncc(){
                var link = document.querySelectorAll('[href="/products/dtf-gang-sheets"]');
                  if(link.length>0) {
                    link[0].remove();
                    // clearInterval(mytt);
                  }
              }
             function mydis (){
              let addbuttons= document.querySelectorAll('[type="button"][aria-label="Increase item quantity by one"]')
               for(let i=0;i<addbuttons.length;i++){
                 addbuttons[i].disabled=true;
                 addbuttons[i].style.cursor="not-allowed"
               }
                let minusbuttons= document.querySelectorAll('[type="button"][aria-label="Reduce item quantity by one"]')
               for(let i=0;i<minusbuttons.length;i++){
                 minusbuttons[i].disabled=true;
                 minusbuttons[i].style.cursor="not-allowed"
               }

               let updatetextbox=document.querySelectorAll('[type="text"][name="updates[]"]')
               for(let i=0;i<updatetextbox.length;i++){
                 updatetextbox[i].disabled=true;
                 updatetextbox[i].style.cursor="not-allowed"
               }
             }
             function myen(){
                 let addbuttons= document.querySelectorAll('[type="button"][aria-label="Increase item quantity by one"]')
                 for(let i=0;i<addbuttons.length;i++){
                   addbuttons[i].disabled=false;
                   addbuttons[i].style.cursor="pointer"
                 }
                  let minusbuttons= document.querySelectorAll('[type="button"][aria-label="Reduce item quantity by one"]')
                 for(let i=0;i<minusbuttons.length;i++){
                   minusbuttons[i].disabled=false;
                   minusbuttons[i].style.cursor="pointer"
                 }

                 let updatetextbox=document.querySelectorAll('[type="text"][name="updates[]"]')
                 for(let i=0;i<updatetextbox.length;i++){
                   updatetextbox[i].disabled=false;
                   updatetextbox[i].style.cursor="pointer"
                 }
             }
             function isJsonString(str) {
                  try {
                      JSON.parse(str);
                  } catch (e) {
                      return false;
                  }
                  return true;
              }

              function isNumeric(str) {
                if (typeof str != "string" && typeof str != 'number') return;
                return !isNaN(str) &&
                       !isNaN(parseFloat(str))
              }

              //Hamed's codes
              function addInches(){
                setTimeout(function(){
                    let qtyWrapper=document.querySelectorAll(".js-qty__wrapper");
                    if(qtyWrapper.length>0){
                      for(let i=0;i<qtyWrapper.length;i++){
                        if(qtyWrapper[i].closest("div.cart__item-sub").closest("div.cart__item-details").querySelector('[data-product_id="' + pid + '"]')!=null){
                             if(qtyWrapper[i].parentElement.getElementsByClassName("inchspan").length==0){
                                var txt=document.createElement("span");
                                 txt.classList.add("inchspan");
                                 txt.innerHTML="Inch(es)";
                                qtyWrapper[i].parentNode.insertBefore(txt,qtyWrapper[i].nextSibling)
                              }
                        }


                      }

                    }
                },2000)
              }
            function DisableButtons(){
               if(window.location.href.includes("/cart")){
                 setTimeout(function(){
                        let qtyWrapper=document.querySelectorAll(".js-qty__wrapper");
                        if(qtyWrapper.length>0){
                          for(let i=0;i<qtyWrapper.length;i++){
                              qtyWrapper[i].style.pointerEvents="none"
                              qtyWrapper[i].style.cursor="not-allowed"
                            }
                        }
                  },2000)
               }
            }


            var pid = '7453184196656';
            document.querySelector(".site-nav__link.site-nav__link--icon.js-drawer-open-cart").onclick=(function (){addInches()})
            document.addEventListener('DOMContentLoaded', function() {
                  if(window.location.href.includes("/cart")){
                    addInches()
                       DisableButtons()
                  }
                  else{
                      document.querySelector("button[name='add']").onclick=(function (){addInches()})
                      document.querySelector("form#CartDrawerForm").onsubmit=(function(e){

                      })
                  }
                  const observer = new MutationObserver(callback);
                        observer.observe(document.querySelector("#CartDrawer div.appear-animation.appear-delay-2"), {
                          subtree: true,
                          childList: true,
                        });

                  }, false);
            document.onload=(function(){

            })
            function callback(mutation){
              if(mutation[mutation.length-1].addedNodes.length>0){
                if(mutation[mutation.length-1].addedNodes[0].getAttribute("class").includes("cart__items")){
                  let qtyWrapper=mutation[mutation.length-1].addedNodes[0].querySelectorAll(".js-qty__wrapper");
                  if(qtyWrapper.length>0){
                      for(let i=0;i<qtyWrapper.length;i++){
                            qtyWrapper[i].style.pointerEvents="none"
                            qtyWrapper[i].style.cursor="not-allowed"
                      }
                  }
                }
              }
            }

            // when cart button on top right corner clicked
            var carto = document.querySelector('.js-drawer-open-cart');
            carto.addEventListener("click", function(e) {
                setTimeout(function() {
                    mydis();
                }, 2000);
            }, false);

              var proxied = window.XMLHttpRequest.prototype.send;
              window.XMLHttpRequest.prototype.send = function() {
                  var pointer = this
                  if(pointer._url.indexOf('upload')!==-1){
                      if(pointer._method.toUpperCase() === 'POST'){
                          console.log(pointer._method + ': ' + pointer._url)
                          console.log( arguments );
                          if(arguments.length>0){
                              var mainarg = arguments[0];
                              if(isJsonString(mainarg)){
                                  var myjson = JSON.parse(mainarg);
                                  if(myjson.hasOwnProperty('meta')){
                                      if(myjson.meta.hasOwnProperty('height')){
                                         if(isNumeric(myjson.meta.height)){
                                           var size = Math.round(myjson.meta.height/300);
                                           if(window.hasOwnProperty('mysize')){
                                             if(isNumeric(size)){
                                               if(isNumeric(window.mysize)){
                                                 window.localStorage.setItem('mysize', window.localStorage.getItem('mysize')+size);
                                                 window.mysize += size;
                                               }else{
                                                 window.localStorage.setItem('mysize', size);
                                                 window.mysize = size;
                                               }
                                             }
                                           }else{
                                               window.localStorage.setItem('mysize', size);
                                               window.mysize = size;
                                           }
                                           myen();

                                           document.querySelectorAll('[type="submit"][name="add"]')[0].addEventListener("click", function(){
                                             setTimeout(function(){
                                               document.querySelectorAll('[type="text"][name="updates[]"]')[0].value = window.mysize - 1;
                                               var linkPlus = document.querySelectorAll('[type="button"][aria-label="Increase item quantity by one"]')[0];
                                               linkPlus.click();
                                               setTimeout(function(){
                                                 mydis();
                                                 //if( document.querySelectorAll('[class="original_price"]').length>0)
                                                 //  document.querySelectorAll('[class="original_price"]')[0].innerHTML = 'Inch(es)';
                                                 if(document.querySelector("span.inchspan")==null){
                                                    var txt=document.createElement("span");
                                                 txt.classList.add("inchspan");
                                                 txt.innerHTML="Inch(es)";
                                                 document.querySelector('[class="js-qty__wrapper"]').parentNode.insertBefore(txt,document.querySelector('[class="js-qty__wrapper"]').nextSibling)
                                                 }

                                               },1500);
                                               if(document.querySelectorAll('[type="button"][aria-label="Reduce item quantity by one"]').length>1){
                                                 setTimeout(function(){
                                                     document.querySelectorAll('[type="text"][name="updates[]"]')[document.querySelectorAll('[type="text"][name="updates[]"]').length-1].value = 1;
                                                     var linkMinus = document.querySelectorAll('[type="button"][aria-label="Reduce item quantity by one"]')[document.querySelectorAll('[type="button"][aria-label="Reduce item quantity by one"]').length-1];
                                                     linkMinus.click();
                                                     var timer = setInterval(myFunction, 50);
                                                     function myFunction(){
                                                        if(document.querySelectorAll('[class="cart__item"]').length==1) {
                                                          clearInterval(timer);
                                                          setTimeout(function(){
                                                              mydis();
                                                              if(document.querySelector("span.inchspan")==null){
                                                                 var txt=document.createElement("span");
                                                              txt.classList.add("inchspan");
                                                               txt.innerHTML="Inch(es)";
                                                               document.querySelector('[class="js-qty__wrapper"]').parentNode.insertBefore(txt,document.querySelector('[class="js-qty__wrapper"]').nextSibling)
                                                              }
                                                          },4000);
                                                          return;
                                                        }
                                                     }
                                                 },500);
                                               }
                                             },2000);
                                           });
                                           console.log("mysize: "+window.mysize);
                                         }
                                      }
                                  }
                              }
                          }
                      }
                  }
                  var intervalId = window.setInterval(function(){
                          if(pointer.readyState != 4){
                                  return;
                          }
                          clearInterval(intervalId);
                  }, 1);
                  return proxied.apply(this, [].slice.call(arguments));
              };
          })();
        </script>
      {% else %}

      {% endif %}
    {% else %}
      <script>
        (function() {
            var mytt1 = setInterval(myfunc, 50);
            function myfunc(){
              var link = document.querySelectorAll('[href="/products/dtf-gang-sheet-pro"]');
                if(link.length>0) {
                  link[0].remove();
                }
            }
        })();
      </script>
    {% endif %}

    {%- comment -%}
      --------------------------------------------------------------------------------------------------------------------
      PRO PRODUCT AREA END
      --------------------------------------------------------------------------------------------------------------------
    {%- endcomment -%}

    {%- comment -%}
      --------------------------------------------------------------------------------------------------------------------
      SECONDARY NAVIGATION AREA
      --------------------------------------------------------------------------------------------------------------------
    {%- endcomment -%}

    <nav class="header__secondary-nav" aria-label="{{ 'header.general.secondary_navigation' | t | escape }}">
      {%- liquid
        if section.settings.show_country_selector and localization.available_countries.size > 1
          assign show_country_selector = true
        endif

        if section.settings.show_locale_selector and localization.available_languages.size > 1
          assign show_locale_selector = true
        endif
      -%}
      {% if section.settings.custom_show_icons %}
        <div class="Result" id="Result">
          <div class="divresult">
            <span>Talk to a FastDTF</span>
            <span style="font-weight: bold;"> +1 (201) 250-5146 </span>
          </div>
          {% if customer %}
            <a
              class="divresult"
              href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}"
            >
              <span>Hello {{ customer.first_name }}</span>
              <span style="font-weight: bold;">Welcome to Fast DTF Print!</span>
            </a>

            <a
              class="divresultsvg"
              href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}"
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="2em" height="2em" viewBox="0 0 640 512">
                <path fill="black" d="M610.5 373.3c2.6-14.1 2.6-28.5 0-42.6l25.8-14.9c3-1.7
                  4.3-5.2 3.3-8.5c-6.7-21.6-18.2-41.2-33.2-57.4c-2.3-2.5-6-3.1-9-1.4l-25.8
                  14.9c-10.9-9.3-23.4-16.5-36.9-21.3v-29.8c0-3.4-2.4-6.4-5.7-7.1c-22.3-5-45-4.8-66.2
                  0c-3.3.7-5.7 3.7-5.7 7.1v29.8c-13.5 4.8-26 12-36.9 21.3l-25.8-14.9c-2.9-1.7-6.7-1.1-9 1.4c-15
                  16.2-26.5 35.8-33.2 57.4c-1 3.3.4 6.8 3.3 8.5l25.8 14.9c-2.6 14.1-2.6 28.5 0 42.6l-25.8
                  14.9c-3 1.7-4.3 5.2-3.3 8.5c6.7 21.6 18.2 41.1 33.2 57.4c2.3 2.5 6 3.1 9 1.4l25.8-14.9c10.9 9.3
                  23.4 16.5 36.9 21.3v29.8c0 3.4 2.4 6.4 5.7 7.1c22.3 5 45 4.8 66.2 0c3.3-.7 5.7-3.7
                  5.7-7.1v-29.8c13.5-4.8 26-12 36.9-21.3l25.8 14.9c2.9 1.7 6.7 1.1 9-1.4c15-16.2 26.5-35.8
                  33.2-57.4c1-3.3-.4-6.8-3.3-8.5zM496 400.5c-26.8 0-48.5-21.8-48.5-48.5s21.8-48.5 48.5-48.5s48.5 21.8
                  48.5 48.5s-21.7 48.5-48.5 48.5M224 256c70.7 0 128-57.3 128-128S294.7 0 224 0S96 57.3 96 128s57.3 128
                  128 128m201.2 226.5c-2.3-1.2-4.6-2.6-6.8-3.9l-7.9 4.6c-6 3.4-12.8 5.3-19.6 5.3c-10.9
                  0-21.4-4.6-28.9-12.6c-18.3-19.8-32.3-43.9-40.2-69.6c-5.5-17.7 1.9-36.4 17.9-45.7l7.9-4.6c-.1-2.6-.1-5.2
                  0-7.8l-7.9-4.6c-16-9.2-23.4-28-17.9-45.7c.9-2.9 2.2-5.8 3.2-8.7c-3.8-.3-7.5-1.2-11.4-1.2h-16.7c-22.2
                  10.2-46.9 16-72.9 16s-50.6-5.8-72.9-16h-16.7C60.2 288 0 348.2 0 422.4V464c0 26.5 21.5 48 48 48h352c10.1
                  0 19.5-3.2 27.2-8.5c-1.2-3.8-2-7.7-2-11.8z"/>
              </svg>
            </a>

          {% else %}
            <a
              class="divresult"
              href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}"
            >
              <span>Hello, sign in</span>
              <span style="font-weight: bold;">Login / Reorder</span>
            </a>

            <a
              class="divresultsvg"
              href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="2em"
                height="2em"
                viewBox="0 0 24 24"
              >
                <path fill="black" d="M6 8c0-2.21 1.79-4 4-4s4 1.79 4 4s-1.79 4-4 4s-4-1.79-4-4m6
                  10.2c0-.96.5-1.86 1.2-2.46v-.24c0-.39.07-.76.18-1.12c-1.03-.24-2.17-.38-3.38-.38c-4.42 0-8
                  1.79-8 4v2h10zm10 .1v3.5c0 .6-.6 1.2-1.3 1.2h-5.5c-.6 0-1.2-.6-1.2-1.3v-3.5c0-.6.6-1.2 1.2-1.2v-1.5c0-1.4
                  1.4-2.5 2.8-2.5s2.8 1.1 2.8 2.5V17c.6 0 1.2.6 1.2 1.3m-2.5-2.8c0-.8-.7-1.3-1.5-1.3s-1.5.5-1.5 1.3V17h3z"/>
              </svg>
            </a>
          {% endif %}
        </div>
      {% endif %}
      {%- if show_country_selector or show_locale_selector -%}
        <div class="localization-selectors md-max:hidden">
          {%- if show_country_selector -%}
            {%- render 'localization-selector',
              type: 'country',
              placement: 'bottom-end',
              show_country_name: section.settings.show_country_name,
              show_country_flag: section.settings.show_country_flag,
              id_prefix: 'header-nav'
            -%}
          {%- endif -%}

          {%- if show_locale_selector -%}
            {%- render 'localization-selector', type: 'locale', placement: 'bottom-end', id_prefix: 'header-nav' -%}
          {%- endif -%}
        </div>
      {%- endif -%}

      {%- comment -%}Offer a hook for apps to integrate into the header to add their own icon, for instance{%- endcomment -%}
      {%- for block in section.blocks -%}
        {%- if block.type == '@app' -%}
          {%- render block -%}
        {%- endif -%}
      {%- endfor -%}

      {%- if shop.customer_accounts_enabled -%}
        <a
          href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}"
          class="sm-max:hidden svgpro"
        >
          <span class="sr-only">{{ 'header.general.open_account' | t }}</span>

          {%- if section.settings.show_icons -%}
            {%- render 'icon' with 'account', class: 'header__nav-icon' -%}
          {%- else -%}
            <span class="md:hidden">{%- render 'icon' with 'account', class: 'header__nav-icon' -%}</span>

            <span class="h6 link-faded md-max:hidden">
              {%- if customer -%}
                {{- 'header.general.account' | t -}}
              {%- else -%}
                {{- 'header.general.login' | t -}}
              {%- endif -%}
            </span>
          {%- endif -%}
        </a>
      {%- endif -%}

      {%- if section.settings.enable_search -%}
        <a href="{{ routes.search_url }}" aria-controls="header-search-{{ section.id }}">
          <span class="sr-only">{{ 'header.general.open_search' | t }}</span>

          {%- if section.settings.show_icons -%}
            {%- render 'icon' with 'search', class: 'header__nav-icon' -%}
          {%- else -%}
            <span class="md:hidden">{%- render 'icon' with 'search', class: 'header__nav-icon' -%}</span>
            <span class="h6 link-faded md-max:hidden">{{ 'header.general.search' | t }}</span>
          {%- endif -%}
        </a>
      {%- endif -%}

      <a
        href="{{ routes.cart_url }}"
        class="relative"
        {% if settings.cart_type != 'page' and request.page_type != 'cart' %}
          aria-controls="cart-drawer"
        {% endif %}
      >
        <span class="sr-only">{{ 'header.general.open_cart' | t }}</span>

        {%- if section.settings.show_icons -%}
          {%- render 'icon' with 'cart', class: 'header__nav-icon' -%}
        {%- else -%}
          <span class="md:hidden">{%- render 'icon' with 'cart', class: 'header__nav-icon' -%}</span>
          <span class="h6 link-faded md-max:hidden">
            {{- 'header.general.cart' | t }} (<cart-count>{{ cart.item_count }}</cart-count>)</span
          >
        {%- endif -%}

        <cart-dot
          class="header__cart-dot {% unless cart.empty? %}is-visible{% endunless %} {% unless section.settings.show_icons %}md:hidden{% endunless %}"
        ></cart-dot>
      </a>
    </nav>

    {%- if section.settings.enable_search -%}
      {%- render 'header-search' -%}
    {%- endif -%}

    {%- assign sidebar_menu = section.settings.sidebar_menu | default: section.settings.menu -%}

    {%- if sidebar_menu.links.size > 0 -%}
      {%- render 'header-sidebar',
        menu: sidebar_menu,
        color_scheme: section.settings.sidebar_color_scheme,
        show_dividers: section.settings.sidebar_show_dividers
      -%}
    {%- endif -%}
  </x-header>
</height-observer>
<script>
  document.documentElement.style.setProperty('--header-height', `${document.getElementById('shopify-section-{{ section.id }}').clientHeight.toFixed(2)}px`);
</script>

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "shopify-section--header",
  "tag": "header",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:global.colors.scheme",
      "default": "scheme-2"
    },
    {
      "type": "checkbox",
      "id": "enable_full_width",
      "label": "t:sections.header.enable_full_width",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "enable_sticky",
      "label": "t:sections.header.enable_sticky_header",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "hide_on_scroll",
      "label": "t:sections.header.hide_on_scroll",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_separation_border",
      "label": "t:sections.header.show_separation_border",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "reduce_padding",
      "label": "t:sections.header.reduce_padding",
      "default": false
    },
    {
      "type": "header",
      "content": "t:sections.header.logo_category"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "t:global.image.image",
      "info": "t:sections.header.logo_size_recommendation"
    },
    {
      "type": "range",
      "id": "logo_max_width",
      "min": 20,
      "max": 300,
      "step": 5,
      "unit": "px",
      "label": "t:global.image.width",
      "default": 140
    },
    {
      "type": "range",
      "id": "logo_mobile_max_width",
      "min": 20,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "t:global.image.mobile_width",
      "default": 100
    },
    {
      "type": "header",
      "content": "t:sections.header.navigation_category"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:sections.header.menu",
      "default": "main-menu"
    },
    {
      "type": "link_list",
      "id": "sidebar_menu",
      "label": "t:sections.header.mobile_menu"
    },
    {
      "type": "select",
      "id": "menu_open_trigger",
      "label": "t:sections.header.menu_open_trigger",
      "info": "t:sections.header.menu_open_trigger_info",
      "options": [
        {
          "value": "hover",
          "label": "t:sections.header.menu_open_trigger_options.hover"
        },
        {
          "value": "click",
          "label": "t:sections.header.menu_open_trigger_options.click"
        }
      ],
      "default": "hover"
    },
    {
      "type": "checkbox",
      "id": "enable_search",
      "label": "t:sections.header.enable_search",
      "default": true
    },
    {
      "type": "header",
      "content": "t:sections.header.sidebar_navigation_category"
    },
    {
      "type": "color_scheme",
      "id": "sidebar_color_scheme",
      "label": "t:global.colors.scheme",
      "default": "scheme-2"
    },
    {
      "type": "select",
      "id": "sidebar_text_font",
      "label": "t:sections.header.sidebar_text_font",
      "options": [
        {
          "value": "heading",
          "label": "t:sections.header.sidebar_text_font_options.heading"
        },
        {
          "value": "body",
          "label": "t:sections.header.sidebar_text_font_options.body"
        }
      ],
      "default": "heading"
    },
    {
      "type": "checkbox",
      "id": "sidebar_show_dividers",
      "label": "t:sections.header.sidebar_show_dividers",
      "default": true
    },
    {
      "type": "header",
      "content": "t:sections.header.desktop_category"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "t:sections.header.layout",
      "options": [
        {
          "value": "logo_left_navigation_inline",
          "label": "t:sections.header.layout_options.logo_left_navigation_inline"
        },
        {
          "value": "logo_left_navigation_center",
          "label": "t:sections.header.layout_options.logo_left_navigation_center"
        },
        {
          "value": "logo_center_navigation_inline",
          "label": "t:sections.header.layout_options.logo_center_navigation_inline"
        },
        {
          "value": "logo_center_navigation_below",
          "label": "t:sections.header.layout_options.logo_center_navigation_below"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.layout_options.drawer"
        }
      ],
      "default": "logo_left_navigation_inline"
    },
    {
      "type": "checkbox",
      "id": "show_icons",
      "label": "t:sections.header.show_icons",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "custom_show_icons",
      "label": "t:sections.header.custum_show_icons",
      "default": true
    },

    {
      "type": "header",
      "content": "t:global.localization.country_region_selector_category",
      "info": "t:global.localization.country_region_selector_category_info"
    },
    {
      "type": "checkbox",
      "id": "show_country_selector",
      "label": "t:global.localization.show_country_region_selector",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_country_name",
      "label": "t:global.localization.show_country_name",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_country_flag",
      "label": "t:global.localization.show_country_flag",
      "default": true
    },
    {
      "type": "header",
      "content": "t:global.localization.language_selector_category",
      "info": "t:global.localization.language_selector_category_info"
    },
    {
      "type": "checkbox",
      "id": "show_locale_selector",
      "label": "t:global.localization.show_locale_selector",
      "default": false
    },
    {
      "type": "header",
      "content": "t:sections.header.transparent_header_category"
    },
    {
      "type": "paragraph",
      "content": "t:sections.header.transparent_header_info"
    },
    {
      "type": "color",
      "id": "transparent_header_text_color",
      "label": "t:global.colors.text",
      "default": "#ffffff"
    },
    {
      "type": "image_picker",
      "id": "transparent_logo",
      "label": "t:sections.header.transparent_header_logo_image",
      "info": "t:sections.header.transparent_header_logo_size_recommendation"
    }
  ],
  "blocks": [
    {
      "type": "mega_menu",
      "name": "t:sections.header.blocks.mega_menu.name",
      "settings": [
        {
          "type": "text",
          "id": "menu_item",
          "label": "t:sections.header.blocks.mega_menu.menu_item",
          "info": "t:sections.header.blocks.mega_menu.menu_item_info"
        },
        {
          "type": "select",
          "id": "images_position",
          "label": "t:sections.header.blocks.mega_menu.images_position",
          "options": [
            {
              "value": "left",
              "label": "t:global.position.left"
            },
            {
              "value": "right",
              "label": "t:global.position.right"
            }
          ],
          "default": "right"
        },
        {
          "type": "header",
          "content": "t:sections.header.blocks.mega_menu.image_1"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "t:global.image.image",
          "info": "t:sections.header.blocks.mega_menu.image_size_recommendation"
        },
        {
          "type": "inline_richtext",
          "id": "image_1_heading",
          "label": "t:global.text.heading"
        },
        {
          "type": "inline_richtext",
          "id": "image_1_text",
          "label": "t:global.text.text"
        },
        {
          "type": "url",
          "id": "image_1_link",
          "label": "t:global.text.link"
        },
        {
          "type": "header",
          "content": "t:sections.header.blocks.mega_menu.image_2"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "t:global.image.image",
          "info": "t:sections.header.blocks.mega_menu.image_size_recommendation"
        },
        {
          "type": "inline_richtext",
          "id": "image_2_heading",
          "label": "t:global.text.heading"
        },
        {
          "type": "inline_richtext",
          "id": "image_2_text",
          "label": "t:global.text.text"
        },
        {
          "type": "url",
          "id": "image_2_link",
          "label": "t:global.text.link"
        }
      ]
    }
  ]
}
{% endschema %}
